# App

APP_VERSION='local'

# Environment

ENVIRONMENT='development'

# Service

SERVICE_NAME='firecrest-web-ui'

# Company

COMPANY_NAME='MyCompany'
APP_NAME='MyPlatform'
LOGO_PATH='logo.svg'

# Logging

LOGGING_LEVEL='debug'

# Session

SESSION_SECRET='s3cr3t'
SESSION_FILE_SECRET='s3cr3tfile'
SESSION_FILE_DIR_PATH='./sessions'

# Redis

REDIS_ACTIVE=false

# OIDC
# Set OIDC_ISSUER_URL to the full issuer URL of your identity provider.
# All endpoints are discovered automatically from {issuerUrl}/.well-known/openid-configuration.

OIDC_ISSUER_URL='http://localhost:8080/auth/realms/kcrealm'
OIDC_CLIENT_ID='firecrest-web-ui'
OIDC_CLIENT_SECRET='<client-secret>'
OIDC_CALLBACK_URL='http://localhost:3000/auth/callback'
OIDC_POST_LOGOUT_REDIRECT_URL='http://localhost:3000/'

# FirecREST (f7t)

FIRECREST_API_BASE_URL='http://localhost:8000'

# UI

UI_HOSTNAME='localhost:3000'

# URLs
SUPPORT_URL='<support-url>'
DOC_URL='<doc-url>'
REPO_URL='<repo-url>'